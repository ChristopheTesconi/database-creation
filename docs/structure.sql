-- Generated by Mocodo 4.2.3

CREATE TABLE COMMENT (
  PRIMARY KEY (id),
  id      VARCHAR(42) NOT NULL,
  comment VARCHAR(42),
  id_2    VARCHAR(42) NULL,
  id_3    VARCHAR(42) NOT NULL
);

CREATE TABLE ORIGAMI (
  PRIMARY KEY (id),
  id          VARCHAR(42) NOT NULL,
  name        VARCHAR(42),
  description VARCHAR(42),
  created_at  VARCHAR(42),
  updated_at  VARCHAR(42),
  id_2        VARCHAR(42) NOT NULL
);

CREATE TABLE USER (
  PRIMARY KEY (id),
  id       VARCHAR(42) NOT NULL,
  name     VARCHAR(42),
  email    VARCHAR(42),
  password VARCHAR(42)
);

ALTER TABLE COMMENT ADD FOREIGN KEY (id_3) REFERENCES USER (id);
ALTER TABLE COMMENT ADD FOREIGN KEY (id_2) REFERENCES ORIGAMI (id);

ALTER TABLE ORIGAMI ADD FOREIGN KEY (id_2) REFERENCES USER (id);

